// Mirrors backend/models/config.py — not exposed via any API endpoint today,
// so these are static labels rather than fetched values.
export const CHAT_MODEL = "gpt-4o-mini";
export const EMBEDDING_MODEL = "text-embedding-3-small";
